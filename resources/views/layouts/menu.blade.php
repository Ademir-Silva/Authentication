<header>
    <nav class="navbar">
        <div class="container-fluid">
            <img src="{{ url('images/Logo.png') }}" width="100px" class="d-inline-block align-text-top" alt="">
            @yield('menu')
        </div>
    </nav>
</header>
